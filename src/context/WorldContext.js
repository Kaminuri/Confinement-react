import React from "react";
import axios from 'axios';
import sha1 from 'sha1';

export { registerWorld };

function registerWorld(CurrentIdGame, CurrentWorldName, CurrentIdWorldMaster) {
   // var sha1 = require('sha1');
   // let CurrentPassword = sha1(localStorage.getItem('password'));
   // let CurrentEmail    = localStorage.getItem('email');
   // const world = {
   //     idgame: CurrentIdGame,
   //     idworldmaster: CurrentIdWorldMaster,
   //     worldname: CurrentWorldName,
   //     password: CurrentPassword,
   //     email: CurrentEmail,
   //     token: localStorage.getItem('id_token')
   // };
   // let token = "";
   // axios.post(`https://localhost:44314/api/World`, { world })
   // .then(res => {
   //     token = res.data;
   // });
    return true;
}
